# Visual QA

Screenshots:

- `add-link-form.png`: final token-based Link form before save.
- `add-photo.png`: Photo state with native picker and explicit original-file limits.
- `privacy-switcher.png`: app-switcher capture where Clip Inbox is an opaque protected card.
- `add-link-saved.png`: successful durable-save state captured during the interaction pass; the duplicate-location wording visible in this intermediate capture was corrected afterward.

Result:

- Standard-size Add retains the existing warm, flat, list-first visual language.
- Type selection remains direct and compact; the form does not introduce pill clouds, gradients, floating cards, or new decorative chrome.
- Field, button, and navigation targets remain at the existing 44pt minimum.
- No clipping or horizontal overflow was observed on the iPhone 17 Pro viewport.
