# Design Token Check

- Source contract: repository `DESIGN.md` and `Tokens.swift`.
- Add uses `ScreenScaffold`, `ScreenHeader`, `BoardSection`, `TwoRowHorizontalSelection`, `StatePanel`, token surfaces, and existing primary/secondary button styles.
- No new colors, fonts, shadows, radii, spacing scales, or navigation patterns were introduced.
- Yellow remains selection/primary-action emphasis; error copy uses the existing danger token.
- The inactive privacy view uses the warm app background and primary text tokens without exposing clip content.
