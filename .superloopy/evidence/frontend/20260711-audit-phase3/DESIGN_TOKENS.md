# Design Token Check

- First-run guidance uses the existing screen header, state panel, typography, yellow action, hairlines, and flat list rhythm.
- Undo uses the existing yellow primary-action token and 44pt action target.
- Recovery uses green; queue attention uses yellow. Neither relies on color alone because both include an icon, title, and message.
- Storage and backup disclosure use existing `BoardSection`, `StatePanel`, and destination-row patterns.
- No new palette, font, shadow, radius, or navigation token was introduced.
