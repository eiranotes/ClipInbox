# Design Token Receipt

- Contract: `/Volumes/AI/Clip/DESIGN.md`
- Direction: existing compact list-first native iOS settings grammar
- New surface: one `DestinationRow` and one two-choice setting detail
- Tokens reused: `space.settingChoiceTop`, `size.actionTarget`, `size.iconColumn`, `color.accent.yellow`, `color.border.soft`, `type.body`, and existing `ActionRow` selected state
- New raw visual values: none
- New component system: none

The change adds behavior and one list row without changing the established color, type, spacing, radius, depth, or motion scales.
